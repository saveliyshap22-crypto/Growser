name: Inspect v0.5.0 release run

on:
  push:
    paths:
      - .github/workflows/inspect-v050-release.yml

permissions:
  actions: read
  contents: write

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Inspect release workflow
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_SHA: 0d1a91d91d4802f4f6a0cb00c9719dc7a3ba8686
        shell: bash
        run: |
          sleep 30
          curl -fsSL -H "Authorization: Bearer ${GH_TOKEN}" -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/runs?head_sha=${RELEASE_SHA}&per_page=20" \
            | jq '{runs: [.workflow_runs[] | {id, name, event, status, conclusion, created_at, updated_at, html_url}]}' \
            > .release-runs-v050.json
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .release-runs-v050.json
          git commit -m "Record v0.5.0 release workflow status" || true
          git push
