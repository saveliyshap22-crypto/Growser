<UserControl x:Class="Chroma.Browser.Views.BrowserTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             Background="#101217">
    <Grid>
        <wv2:WebView2 x:Name="WebView" />

        <Border x:Name="LoadingSurface"
                Background="#15171B"
                Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar Width="190" Height="3" IsIndeterminate="True" Foreground="{StaticResource AccentBrush}" />
                <TextBlock Margin="0,16,0,0" Foreground="{StaticResource MutedTextBrush}" Text="Загрузка…" />
            </StackPanel>
        </Border>

        <Border x:Name="ErrorSurface"
                Margin="24"
                Padding="28"
                MaxWidth="560"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Background="{StaticResource PanelBrush}"
                BorderBrush="#38FFFFFF"
                BorderThickness="1"
                CornerRadius="24"
                Visibility="Collapsed">
            <StackPanel>
                <TextBlock FontSize="26" FontWeight="SemiBold" Text="Не удалось открыть страницу" />
                <TextBlock x:Name="ErrorText" Margin="0,10,0,20" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" />
                <Button Width="130" HorizontalAlignment="Left" Style="{StaticResource GlassButton}" Click="Retry_Click" Content="Повторить" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

